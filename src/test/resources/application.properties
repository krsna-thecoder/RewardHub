# Test overrides. Disable the real-time feed by default so the scheduled drip
# does not insert transactions during tests. Individual tests that verify feed
# wiring override feed.type via @TestPropertySource.
feed.type=none
